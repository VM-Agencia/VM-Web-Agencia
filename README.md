# 1. Si no lo has hecho antes, inicializa Git
git init

# 2. Crea el enlace (reemplaza la URL por la que copiaste)
git remote add origin https://github.com/VM-Agencia/VM-Web-Agencia.git

# 3. (Opcional) Verifica que el enlace sea correcto
git remote -v